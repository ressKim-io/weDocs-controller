apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# M5에서 서비스 매니페스트 추가:
#   ws-gateway / doc-service / crdt-engine / ai-service / indexer
#   + PostgreSQL(pgvector) / Redis / Kafka (또는 외부 의존성)
resources: []
