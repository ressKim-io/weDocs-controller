# {PROJECT_NAME} — Version Compatibility Matrix

> 의존성 호환성 SoT. K8s/Istio/ArgoCD/Helm chart/언어/SDK 버전 변경 시 반드시 갱신.
> `.claude/rules/version-compatibility.md` 의 기준.

---

## 현재 핵심 제약 ({YYYY-MM-DD} 기준)

```
K8s 타겟:    {1.34}
Istio:       {1.29}
ArgoCD:      {v3.3}
Java:        {21 LTS}
OTel BOM:    {2.25.0}
Helm:        {3.13+}
Container runtime: {containerd 1.7+}
```

---

## 도구별 호환 매트릭스

### Kubernetes API

| API version | 최소 K8s | 최대 K8s | 비고 |
|------------|---------|---------|------|
| apps/v1 | 1.9+ | current | stable |
| networking.k8s.io/v1 NetworkPolicy | 1.19+ | current | egress 지원 |
| {추가...} | | | |

### Istio

| Istio version | 지원 K8s | 종료 예정 | 비고 |
|--------------|---------|----------|------|
| 1.29 | 1.31-1.35 | {YYYY-MM} | current |
| 1.28 | 1.30-1.34 | {YYYY-MM} | LTS |
| {추가...} | | | |

### ArgoCD

| ArgoCD version | tested K8s | 비고 |
|---------------|-----------|------|
| v3.3 | 1.32-1.34 | current |
| v3.2 | 1.31-1.33 | |

### Grafana 스택

| 컴포넌트 | Helm chart repo | 비고 |
|---------|----------------|------|
| Tempo | `grafana-community/helm-charts` | community 이관 |
| Loki | `grafana/helm-charts` | |
| Mimir | `grafana/helm-charts` | |
| Alloy | `grafana/helm-charts` | |
| Pyroscope | `grafana/helm-charts` | |

---

## 언어 / 런타임

### Java

| Java | Spring Boot | Spring Framework | Gradle | 비고 |
|------|------------|-----------------|--------|------|
| 21 LTS | 3.5.x | 6.2.x | 8.x+ | current |
| 17 LTS | 3.3.x | 6.1.x | 8.x | maintenance |

### Go

| Go | 비고 |
|----|------|
| 1.22 | current |
| 1.21 | maintenance |

### Node.js

| Node | TypeScript | 비고 |
|------|-----------|------|
| 20 LTS | 5.x | current |

---

## OpenTelemetry

| SDK 언어 | 버전 | 비고 |
|---------|------|------|
| Java auto-instrumentation agent | {2.10.0} | Spring Boot starter 와 동시 사용 금지 |
| Java Spring Boot starter | {2.25.0} | BOM 사용 필수 |
| Go SDK | {1.30.0} | |
| OTel Collector | {0.108.0} | |

---

## 절대 금지

> 검증되지 않은 버전 조합. 새 버전이 출시되면 검증 후 이 섹션에서 제거.

- {예: K8s 1.35 + ArgoCD v3.3 — protobuf 이슈 #25767}
- {OTel Java Agent + Spring Boot Starter 동시 사용 — 이중 계측}
- {OTel instrumentation BOM 없이 개별 버전 관리}

---

## 작업 전 확인 (MANDATORY)

다음 작업 수행 전 본 문서 갱신 / 확인:

- Helm chart 버전 변경 또는 업그레이드
- K8s manifest 작성/수정 (API version)
- Istio 설정 변경 (values 구조)
- ArgoCD Application/ApplicationSet 수정
- Spring Boot / OTel 의존성 변경
- Grafana 스택 설정 변경
- docker-compose / Dockerfile 베이스 이미지 변경

상세 규칙: `.claude/rules/version-compatibility.md`
